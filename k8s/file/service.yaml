apiVersion: v1
kind: Service
metadata:
  name: aklp-file
  namespace: aklp
  labels:
    app: aklp-file
spec:
  type: NodePort
  selector:
    app: aklp-file
  ports:
    - port: 8004
      targetPort: http
      nodePort: 30004
      protocol: TCP
      name: http